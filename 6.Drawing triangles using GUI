def shape_angles(num_sides):
  angles=360/num_sides
  for _ in range(num_sides):
    timmy_turtle.forward(100)
    timmy_turtle.right(angles)


for shapes in range(3,11):
// if you want diff colors then make a list of colours name and then perform the random function here choice here
  shape_angles(shapes)
  
